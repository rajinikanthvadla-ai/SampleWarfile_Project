@Library('emergencySharedLib') _
pipeline {
    agent any 
    stages{
        stage('start') {
            steps {
                echo "pipeline started!"
                printMessage("Hello from jenkins! sharelib")
            }
        }
    }
}
